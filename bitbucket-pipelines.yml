image: gcc:10.2

pipelines:
  default:
    - step:
        name: Build
        script:
          - apt-get update && apt-get install -y cmake
          - ./build.sh -r
    - step:
        name: Test
        script:
          - apt-get update && apt-get install -y cmake
          - ./build.sh -r -t
